nano genmap.py #Вставить код
cd catkin_ws/src/sitl_gazebo/models #Перейти по пути
git clone nttp://github.com/bart02/dronepoint.git #Загрузить модели в текующую папку(models)
mv dronepoint/dronepoint_blue . #Переместить модель в папку models, ПРОБЕЛ + ТОЧКА НА КОНЦЕ ОБЯЗАТЕЛЬНО!!!!!!!
mv dronepoint/dronepoint_red .
mv dronepoint/dronepoint_green .
mv dronepoint/dronepoint_blue .
rm -rf dronepoint/
#Испольование:
  python3 genmap.py без аргументов #генерация карты
  pytohn3 genmap.py clear #(любой аргумент,хоть строка,хоть число-не важно) - очистка карты от моделей
